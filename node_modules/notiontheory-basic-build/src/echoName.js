var rename = require("gulp-rename"),
  path = require("path");

function echoName(op) {
  return rename(function(dat){
    console.log("%s: %s%s%s%s", op, dat.dirname, path.sep, dat.basename, dat.extname);
    return dat;
  });
}

module.exports = echoName;